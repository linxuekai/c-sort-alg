/**
 * 交换两个 int 指针的值
 * -------------------- */
extern void swap(int *a, int *b);

/**
 * 根据给定的长度 n 和 上下限 min, max
 * 生成随机 int 数组
 * -------------------- */
extern int *genRandomArr(int len, int min, int max);

/**
 * 打印数组
 * -------------------- */
extern void printArr(int *arr, int len);

/**
 * 幂计算
 * -------------------- */
int _pow(int num, unsigned int radix);