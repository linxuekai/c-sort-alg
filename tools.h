/**
 * 交换两个 int 指针的值
 * -------------------- */
extern void swap(int *a, int *b);

/**
 * 根据给定的长度 n 和 上下限 min, max
 * 生成随机 int 数组
 * -------------------- */
extern int *gen_random_arr(int len, int min, int max);

/**
 * 打印数组
 * -------------------- */
extern void print_arr(int *arr, int len);
